# Task System v3.0 Changelog

**Release Date**: 2025-11-09  
**Version**: 3.0

## 概述

本次更新为 FFmpeg Easy 带来了全面的任务管理系统，包括任务历史记录、批处理队列、友好的错误处理和文件名自动清理功能。

## 主要功能

### ✨ 新功能

#### 1. 任务历史记录系统
- **IndexedDB 持久化存储**：使用 Dexie.js 管理任务历史记录
  - 所有执行过的任务自动保存到浏览器本地数据库
  - 支持跨会话访问，页面关闭后数据不丢失
  - 自动清理 30 天前的旧记录

- **任务历史查看器**：
  - 📊 统计信息面板（总任务数、成功率、平均耗时）
  - 🔍 多维度筛选（按状态、时间范围）
  - 📋 详细任务信息展示
  - ❌ 友好的错误信息显示
  - 🗑️ 单个删除或批量清理功能

#### 2. 批处理和队列管理
- **队列处理引擎**：
  - 支持批量添加任务到队列
  - 可配置并发数（1-4 个任务同时处理）
  - 实时监控队列状态和执行进度
  - 支持启动、停止、清空队列操作
  - 单个任务可从队列中移除

- **批量文件上传组件**：
  - 🖱️ 拖拽上传支持
  - 📁 多文件选择（最多 10 个）
  - ✅ 文件类型验证
  - ⚠️ 文件大小警告
  - 🏷️ 自动文件名清理提示

- **页面退出保护**：
  - 任务执行中尝试关闭页面会弹出确认提示
  - 使用浏览器原生 beforeunload API
  - 防止误操作导致任务中断

#### 3. 友好的错误处理
- **错误分类系统**：
  - **可恢复错误**：文件格式问题、参数不支持等
  - **不可恢复错误**：内存不足、运行时崩溃等
  - **用户中止**：主动中止任务

- **智能错误识别**：
  - 自动识别 15+ 种常见错误模式
  - 提供中文错误说明和恢复建议
  - 根据错误类型给出具体解决方案

- **错误恢复机制**：
  - 不可恢复错误自动清理 FFmpeg 实例
  - 避免错误状态传播到下一个任务
  - 提示用户重新加载后继续

#### 4. 文件名自动清理
- **智能清理规则**：
  - 移除中文字符
  - 空格转换为下划线
  - 移除特殊字符
  - 空文件名使用时间戳

- **用户反馈**：
  - 上传时显示清理警告
  - 批量上传组件中标记已清理的文件
  - 显示清理前后的文件名对比

#### 5. WASM 兼容性检测
- **不支持选项检测**：
  - 自动识别 WebAssembly 不支持的 FFmpeg 选项
  - 包括硬件加速、网络协议、设备输入等
  - 执行前给出警告但仍允许尝试

- **支持的检测项**：
  - 硬件加速：-hwaccel, -vaapi_device 等
  - 网络协议：-rtsp_transport, -tcp 等
  - 设备输入：-f dshow, -f avfoundation 等

### 🎨 UI/UX 改进

#### 标签页导航
- 主界面新增三个标签页：
  - **执行**：原有的命令执行界面
  - **队列**：队列管理和批量处理
  - **历史**：任务历史查看器

#### 视觉优化
- 统一的卡片式设计
- 清晰的状态徽章（等待中、执行中、已完成、失败、已中止）
- 响应式布局，支持移动端
- 深色模式完全支持

### 🔧 技术架构

#### 新增核心模块
```
app/
├── types/
│   └── task.ts                 # 任务类型定义
├── services/
│   ├── taskDatabase.ts         # IndexedDB 数据库
│   └── queueProcessor.ts       # 队列处理引擎
├── store/
│   └── taskStore.ts           # 任务状态管理
├── hooks/
│   └── useTaskManager.ts      # 任务管理 Hook
├── utils/
│   └── errorHandling.ts       # 错误处理工具
└── components/
    ├── TaskHistoryViewer.tsx  # 历史查看器
    ├── QueueControlPanel.tsx  # 队列控制面板
    ├── BatchFileUpload.tsx    # 批量上传组件
    └── ui/
        └── tabs.tsx           # 标签页组件
```

#### 依赖变更
- **新增**：`dexie@4.2.1` - IndexedDB 封装库
- **新增**：`@radix-ui/react-tabs@1.1.13` - 标签页组件

#### 代码质量
- ✅ 完全类型安全（TypeScript）
- ✅ 通过 Biome lint 检查
- ✅ 遵循单文件不超过 500 行规范
- ✅ 模块化设计，职责清晰

### 🐛 Bug 修复

#### 已修复问题
1. **任务状态清理**：每次新建任务都会清空当前任务的状态
2. **WASM 指令屏蔽**：识别并警告不支持的 FFmpeg 选项
3. **中止功能**：修复中止从来都不成功的问题（已在之前版本修复）
4. **中文文件名**：自动清理，避免处理失败
5. **错误恢复**：不可恢复错误自动清理 FFmpeg 实例

### 📝 文档更新

#### 新增文档
- **TASK_SYSTEM.md**：完整的功能文档
  - 功能介绍和使用方法
  - 最佳实践建议
  - 常见问题解答
  - 技术实现细节

#### 更新文档
- **TODOS.md**：标记已完成的任务
- **AGENTS.md**：（如需要）更新项目结构说明

## 性能优化

### 内存管理
- 自动清理 Blob URLs 防止内存泄漏
- 页面卸载时释放所有任务结果
- 估算内存使用，提前警告用户

### 存储优化
- IndexedDB 仅存储必要信息
- 输出文件 Blob URLs 仅保存在内存中
- 自动清理 30 天前的历史记录

## 最佳实践

### 批处理建议
- 小文件（< 50MB）：可使用 2-3 并发
- 大文件（> 100MB）：建议使用 1 并发
- 复杂编码（VP9、H.265）：必须使用 1 并发

### 文件大小限制
- 重新编码任务：每个文件 < 200MB
- 仅复制任务：每个文件 < 500MB

## 向后兼容性

✅ 完全向后兼容
- 所有现有功能保持不变
- 新功能为可选增强
- 不影响现有用户工作流

## 已知限制

1. **队列暂停**：目前只支持停止整个队列，不支持暂停
2. **历史导出**：任务历史暂时无法导出，计划在后续版本添加
3. **并发限制**：建议并发数不超过 4，避免内存问题
4. **文件大小**：WebAssembly 内存限制，不建议处理超大文件

## 升级说明

### 自动升级
本次更新为纯前端增强，用户无需任何操作：
1. 刷新页面自动加载新版本
2. 现有数据和设置自动保留
3. 新功能立即可用

### 浏览器要求
- 支持 IndexedDB（所有现代浏览器）
- 支持 SharedArrayBuffer（多线程模式）
- 建议使用最新版 Chrome/Edge/Firefox

## 未来计划

### 短期（v3.1）
- 历史记录导出功能
- 队列暂停/恢复功能
- 更多预设命令

### 中期（v3.2）
- 批处理进度详细显示
- 任务重试机制
- 更智能的内存管理

### 长期
- i18n 多语言支持
- AI 助手接入
- Tauri 客户端版本

## 致谢

感谢所有为本项目贡献代码、提出建议和报告问题的开发者和用户！

---

**完整更新日志**：参见 Git 提交历史  
**详细文档**：参见 TASK_SYSTEM.md  
**问题反馈**：GitHub Issues

*FFmpeg Easy v3.0 - 让视频处理更简单* 🎬
