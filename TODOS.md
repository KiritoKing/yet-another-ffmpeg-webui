# Features

- [x] 重构数据层，将ffmpeg-web页面的数据、逻辑、组件分别抽象开来，数据抽象到store中，逻辑抽象到hooks中，组件则单独封装，避免单文件膨胀 ✅ 2025-11-08
- [x] 拆分所有大于500行的文件（commandUtils.ts 683行，useFFmpegWeb.ts 602行） ✅ 2025-11-09
- [x] 拓展任务系统功能，支持任务记录（执行过的任务保存到indexed-db中，使用Dexie.js），本次执行过的任务结果可以保存到内存中，应用退出或内存溢出时释放 ✅ 2025-11-09
- [x] 支持批处理和队列功能，可以一次性上传多个文件，指定batchsize（同时处理的文件数量）并自动排队处理，也支持异步提交多个任务，在任务执行中时尝试退出页面会调用浏览器api尝试阻止 ✅ 2025-11-09
- [x] 完成表单系统的彻底重构，下线掉之前的输入输出系统 ✅ 2025-11-08
- [x] 删除测试页面，让用户可以直接进入主页面，合并homepage ✅ 2025-11-08
- [x] 编辑各个平台的部署配置，支持 Vercel、Netlify、Cloudflare、Render、Fly.io、Railway、Platform.sh、Docker ✅ 2025-11-08
- [x] 友善的错误处理：针对常见错误给出可读的解释和恢复方案，并区分可恢复错误和不可恢复错误 ✅ 2025-11-09
- [x] 处理中文、空格文件名 ✅ 2025-11-09
- [x] 彻底重构进度/日志/任务调度系统
  - 重构数据层和逻辑层，确保原来的store逻辑和ffmpeg封装能够匹配新的任务架构
  - 合并任务调度和现有的进度/日志系统，所有命令都是用任务系统管理，单个文件操作直接发起并执行任务，多个文件操作需要先提交到任务列表后再执行任务
  - 合并任务面板到执行区域，去掉任务的tab，并且保留现有的日志窗口
  - 给日志加上虚拟滚动机制，使用tanstack-virtual库，你需要先使用context7查询API并使用pnpm安装依赖，使用虚拟滚动后就不要再清空日志了
  - 日志现在信息不够，无法区分日志究竟来自pool中哪个实例
  - 当用户切换新的命令并点击开始时，需要清空任务队列，此时如果队列中有输出的结果视频，需要弹窗提醒用户这部分视频会被清空，需要下载保存
- [ ] 增加新手引导，使用driver.js，并在项目内封装通用能力，方便其他类型引导接入
- [ ] 重构store
  - 将store本体、类型（types）和初始值（default-values）分开，特别是command部分，应该将预设脚本分开维护，如 `store/a/types.ts`, `store/a/index.ts`（store本体）和`store/a/default-values.ts`
  - 重新整理所有的预设脚本：清理所有不支持wasm的脚本，能用动参表示的都用动参表示，能合并的就合并
  - 很多组件传递了没有必要的props，明明可以直接消费store或者hooks，却要通过上级组件传递下来，这是架构上设计的不合理导致的，请合理拆分hook来减少不必要的参数传递
- [ ] CDN选择器：用于解决中国大陆用户的网络问题
  - 内置多个cdn源来加载ffmpeg，可以通过网络连通性和速度进行优先选择，如unpkg, jsdeliver等，内置cdn源支持自定义ffmpeg版本（需要校验是否有该版本，最好做成select）
  - `/public`中包含一个内置版本的，随网页托管来部署，兜底可以选择这个方案，缺点是ffmpeg版本固定
  - 支持用户自定义cdn链接，但需要校验有效性
- [ ] 文档整理：汇总和分类项目现在的文档
  - 在一个目录下管理除了readme和agents的所有文档，分为使用说明、开发说明、changelog和blog四个部分
  - 使用Docusaurus渲染上面的文档
  - 为特定的目录模块（有必要的话）增加AGENTS.md，拆分根目录下的AGENTS.md，增加上下文的聚焦性
- [ ] 完成SPEC kit接入，按本次迭代的最终态确定spec
- [x] 脚手架变更
  - 这个应用没有必要ssr，直接去掉ssr能力，变成纯csr应用
  - 改成真正的文件路由模式，你可以先用context7查一下react-router-7最新的api


# Bugs

- [x] 一失败就直接退出到主界面，希望可以保留当前错误信息，由用户主动退出重新加载 ✅ 2025-11-08
- [x] 经常报错 执行失败: ErrnoError: FS error ✅ 2025-11-08
- [x] 任务执行完成后切换命令，按钮状态不会刷新，无法执行下一个任务 ✅ 2025-11-08
- [x] 当日志里有错误时，日志面板无法被折叠 ✅ 2025-11-08
- [x] 重新加载ffmpeg并没有清空状态，现已使用 resetAll() 完整清空所有状态 ✅ 2025-11-08
- [x] 任务系统拓展完成后，每次新建任务都清空当前任务的状态 ✅ 2025-11-09
- [x] 有一些wasm不支持的指令要屏蔽掉 ✅ 2025-11-09
- [x] 中止从来都不成功 ✅ 2025-11-09 (已在之前的更新中修复)
- [x] 任务历史功能好像直接消失了，请你恢复他的入口 ✅ 2025-11-09
- [x] 不要在所有情况下都返回 ❌ 文件名包含空格可能导致问题 💡 建议： 1. 建议使用下划线或连字符代替空格 2. 系统会自动处理文件名 这个错误，比如手动终止的时候就不要显示这个错误了 ✅ 2025-11-09
- [x] 优化任务历史记录时机：创建时立即添加，状态变化时更新 ✅ 2025-11-09

# Milestones

> 后续迭代预计使用SpecKit支持

- i18n多语言支持
- AI助手接入
- Tauri客户端接入
- 浏览器原生高性能：正确的架构通常是 **ffmpeg.wasm（用于解复用/复用）+ WebCodecs（用于编解码）+ WebGPU（用于处理特效）

# Intentions

不要忘记项目的初衷：让中国的小白和初级用户使用ffmpeg操作视频变得简单：

- 抹平网络障碍：让国内小伙伴也能使用
- 抹平语言和交互障碍
- 尽可能降低使用门槛