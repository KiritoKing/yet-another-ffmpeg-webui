/**
 * Theme Store Default Values
 * 主题 Store 默认值
 */

import type { ThemeState } from "./types";

export const defaultThemeState: ThemeState = {
	theme: "system",
	resolvedTheme: "light",
	isClient: false,
};
